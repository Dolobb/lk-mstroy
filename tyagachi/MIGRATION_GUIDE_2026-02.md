# Миграция на новый метод выгрузки путевых листов

**Дата:** Февраль 2026
**Версия:** 2.1

---

## 📋 Что изменилось

Основной метод выгрузки путевых листов заменён с `getRouteLists` на `getRouteListsByDateOut`.

### Ключевые различия

| Параметр | Старый метод (`getRouteLists`) | Новый метод (`getRouteListsByDateOut`) |
|----------|-------------------------------|----------------------------------------|
| **Фильтрация** | По дате закрытия (`closeList`) | По дате выезда (`dateOut`) |
| **Статусы** | Только CLOSED | ВСЕ (CLOSED, PRINTING, NOTUSED, GIVED_BACK, CREATE) |
| **Поле glonassData** | ✅ Присутствует | ❌ Отсутствует |
| **Использование** | Бухгалтерия, отчётность | Планирование, анализ по дням |

---

## 🎯 Зачем это нужно

### Проблема старого метода

При выгрузке ПЛ за период **30.01-04.02** старый метод возвращал:
- 1363 записи
- Только закрытые ПЛ (CLOSED)
- С датами выезда от **01.01** до **31.01** (не попадающими в указанный период!)
- ПЛ с выездом 30.01-04.02, которые ещё не закрыты, **не попадали в выгрузку**

### Решение новым методом

Новый метод за тот же период возвращает:
- 2000 записей
- ВСЕ статусы
- Даты выезда точно в периоде **30.01-04.02**
- Все ПЛ с выездом в указанный период, независимо от статуса

---

## 🚀 Что изменилось в коде

### 1. CLI (консольная команда `--fetch`)

**Автоматически использует новый метод:**

```bash
# Теперь использует getRouteListsByDateOut по умолчанию
python main.py --fetch --from-pl 30.01.2026 --to-pl 04.02.2026
```

### 2. Веб-интерфейс

**Добавлен переключатель:**

В форме создания отчёта появился checkbox:
- ☐ **Использовать старый метод (фильтр по дате закрытия ПЛ)**

По умолчанию выключен (используется новый метод).

### 3. API методы

**APIClient теперь имеет три метода:**

```python
# Универсальный метод (рекомендуется)
client.get_route_lists(from_date, to_date, use_legacy=False)

# Новый метод (явный вызов)
client.get_route_lists_by_date_out(from_date, to_date)

# Старый метод (явный вызов)
client.get_route_lists_legacy(from_date, to_date)
```

**DataFetcher:**

```python
fetcher.fetch_all(
    from_requests='30.01.2026',
    to_requests='04.02.2026',
    from_pl='30.01.2026',
    to_pl='04.02.2026',
    use_legacy_pl_method=False  # False = новый метод (по умолчанию)
)
```

---

## 📝 Рекомендации по использованию

### Используйте новый метод (по умолчанию) когда нужно:
- ✅ Получить все ПЛ по дате выезда
- ✅ Анализировать работу транспорта по дням
- ✅ Видеть незакрытые ПЛ (PRINTING, NOTUSED)
- ✅ Планирование и ежедневная отчётность

### Используйте старый метод когда нужно:
- ✅ Только закрытые ПЛ для бухгалтерии
- ✅ Данные ГЛОНАСС (поле `glonassData`)
- ✅ Фильтрация по дате закрытия ПЛ
- ✅ Финансовая отчётность

---

## 🧪 Тестирование

Создан тестовый скрипт для сравнения методов:

```bash
python tests/compare_route_list_methods.py
```

Результаты тестирования сохраняются в `Data/test_comparison/` с детальным анализом различий.

---

## 🔄 Обратная совместимость

- ✅ Старый метод полностью сохранён (`get_route_lists_legacy`)
- ✅ Веб-интерфейс позволяет выбрать старый метод через checkbox
- ✅ Все существующие скрипты продолжат работать

---

## 📚 Обновлённая документация

- `ARCHITECTURE.md` — обновлены разделы про APIClient и DataFetcher
- Добавлена таблица сравнения методов в начале документа
- Комментарии в коде обновлены

---

## ❓ FAQ

### В: Нужно ли менять существующие скрипты?
**О:** Нет, если вам подходит новый метод. Для использования старого метода добавьте `use_legacy_pl_method=True`.

### В: Где посмотреть какой метод используется?
**О:** В логах отобразится:
- `Fetched N route lists (by DateOut)` — новый метод
- `Fetched N route lists (legacy method)` — старый метод

### В: Можно ли вернуть старый метод по умолчанию?
**О:** Да, в `APIClient.get_route_lists()` измените значение `use_legacy` по умолчанию на `True`.

---

## 📞 Контакты

При возникновении вопросов или проблем обращайтесь к команде разработки.

**Дата миграции:** 05.02.2026
