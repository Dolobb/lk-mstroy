vehicle-status/server/creds.json
node_modules/
dist/
build/
.env
.env.local
.env.*.local
*.log
*.jsonl
*.jsonl.*
.DS_Store
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
Data/
*.db
dumps/
*.dump

# ephemeral / debug artefacts
_debug_*.js
_debug_*.ts
*.tsbuildinfo

# AI Reports — сгенерированные файлы отчётов и данные пользователя
ai-reports/server/output/
ai-reports/server/data/

# screenshots dropped into repo root during notes/reviews
/Снимок экрана*.png
/Screenshot*.png
/imporoving design.png
/glm-ask.png

# office lock files
.~$*
.~*.xlsx
/sample_*.xlsx

# ad-hoc planning notes committed without extension
/план тут
/изменения плана
/готова админка
/готова администратора

# Презентация-кейс (не для репозитория)
/presentation/
/референсы из тиса/

# Claude Code machine-local settings
.claude/settings.local.json

# Файлы, случайно попавшие в корень
/New Folder With Items/
/index (2).html

# ── Локальная аналитика TIS (НПС/ДиМ/КИП) — только на этой машине ──
# Единая локальная папка: кэш getMonitoringStats + временные дампы/данные
/_local/
# подстраховка, если что-то снова запишется в корень
/comparison-cache/
/nps_cookies.txt
/nps_*.json
/nps-passports-raw.local.json
/sensordata.json
/fetch_nps.log

# скрипты выгрузки/сборки коммитятся (токен вынесен в _local/nps_token.txt);
# *.local.js остаются локальными (могут содержать секреты)
/*.local.js

# сгенерированные отчёты — хранить локально в корне, не в репозитории
/КИП-ДиМ-*.xlsx
/Показатели НПС*.xlsx
/kip-dim-*.csv
/kip-dim-*.xlsx
/tis-samosvaly-*.csv
/Для расчета показателей.xlsx

# data-дампы аналитики под kip/config (в репозиторий не нужны)
/kip/config/dim-fuelnorm-coverage.json
/kip/config/dim-vehicle-registry-matches.json
/kip/config/vehicle-registry-passport-matches.json
/kip/config/FUELNORM-COVERAGE-REPORT.md

# Excel lock-файлы (~$...) в корне
~$*
