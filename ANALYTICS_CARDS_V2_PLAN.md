# Handoff / План — Карточки Аналитики v2.0 («Навигация по дням»)

> Документ под холодный старт: ничего не предполагает из диалога. Дата сессии: **2026-06-09**.
> Ветка: `agent/glm/fix-analytics-cards-performance`. Изменения в рабочем дереве, не закоммичены.
> Связанный, но **отдельный** документ: `ANALYTICS_PERF_HANDOFF.md` — перф-оптимизация v1 (другая задача).

---

## 0. Протокол кодовой базы (прочитать первым)

`CLAUDE.md`: **прежде чем читать исходники — проверь документацию.**
Порядок: `Obsidian vault → NAVIGATION.md → <сервис>/docs/*.md → исходники`.
Брейншторм-материалы в vault: `Brainstorm/BRAINSTORM_INDEX.md` + `Brainstorm/samosvaly-ideas-40.md` (40 структурированных идей).
Память-указатель: `brainstorm-analytics-index`.

---

## 1. Контекст и гипотеза

Вид «Карточки» (v1): каждая карточка несёт собственные пер-дневные чипы смен и неявно зависит от
глобального date-range. Гипотеза пользователя:

> **Просмотр ТС по дням — это задача вида «Таблица», а не «Карточки».**
> Карточки не должны дублировать выбор дат внутри каждой ТС. Вместо этого — единая навигация по
> дням сверху, а каждая карточка показывает статистику ТС за выбранный отрезок, красиво и плотно.

Тестируем гипотезу **без риска для v1**: добавляем **4-й вид «Карточки v2»** рядом с
Таблица / Карточки / Карта. Откат = удалить новые файлы + ~4 правки в `AnalyticsPage.tsx`.

---

## 2. Зафиксированные решения (locked в сессии)

- **Смены — единица времени, НЕ фильтр.** В бизнес-реалиях смены идут последовательно как дни
  (день ≈ 12 ч). Фильтра смен НЕТ. Таймлайн в 2× длиннее: 14 дней → **28 ячеек** (C1/C2),
  визуально сгруппированы по дням. Выбор = одна ячейка (день + смена).
- **Вариант A для v2.0:** верхний блок = таймлайн-навигатор, заменяет **только** daterangepicker.
  Тип-фильтры остаются в общем filterbar как сейчас. (Матрица-B с интеграцией групп-фильтров → v2.1, см. §6.)
- **Объект — в правом сайдбаре** (переиспользуем существующий object-selection из map-view).
  Верхний рейтинг объектов отложен — позже как фича на все 4 view.
- **Оба спарклайна:** (a) верхний таймлайн = флот %КИП по сменам; (b) личный спарклайн КИП на
  каждой карточке (28 точек, ось совпадает с таймлайном, выбранная смена — маркером).
- **Бэкенд не трогаем** — все данные уже в `UnifiedRecord` (см. §4).
- **v2 владеет собственным фетчем** на окно 14 дней (заканчивается выбранной сменой; дефолт —
  последняя доступная). Глобальный date-picker в v2 скрыт. Полностью изолировано от v1.
- **Состояния карточки:** данные по нулям / не работала → «не работал»; смена есть, но
  рейсы/диаграмма не выгружены → серая пустая диаграмма + кнопка «Выгрузить» по центру
  (точечная довыгрузка — существующий метод).
- **Откладываем:** полный стек «работа / ход / **простой**» — до DST-fix с холостым ходом (в v2.0
  для самосвалов максимум «в движении vs на объекте»); дельта `↑+8%` к прошлому периоду (нужен
  2-й фетч); большой DST-фикс выгрузки и тонкая визуалка — отдельные сессии.

---

## 3. Анатомия вида v2.0 (вариант A)

### 3.1 Топ-блок — навигатор день×смена

```
┌ ТОП: навигатор день×смена (заменяет только daterangepicker) ──────┐
│         26 ───  27 ───  28 ──  …  ──  08          ← дни            │
│         C1 C2   C1 C2   C1 C2        C1[C2]        ← смены         │
│ Флот    ▇  ▃    ▆  ▅    ▇  ▆          ▇ [▇]        ← %КИП (цвет)    │
│         12 14   13 11   …                          ← сколько ТС    │
└────────────────────────────────────────────────────────────────────┘
```
- 28 ячеек = 14 дней × {C1, C2}, сгруппированы под дне-лейблами с мелкими гэпами между днями.
- Каждая ячейка: цвет = флот %КИП этой смены (пороги green→amber→red), подпись = сколько ТС работало.
- Клик = выбор смены → карточки ниже обновляются. Дефолт = последняя доступная смена (правый край).
- Заменяет **только** daterangepicker; глобальные тип-фильтры и поиск остаются.

### 3.2 Карточка v2 (места больше — убрали пер-карточный выбор дат)

```
┌────────────────────┐
│▌╭91%╮ А123БВ72      │  ← гейдж-кольцо КИП (герой) + health-стрипа слева
│▌╰───╯ КамАЗ·📍Запад │  ← модель + объект (objectName, раньше скрыт)
│▌ ▁▂▃▅▇▆ личн.тренд  │  ← личный спарклайн КИП (28 смен), маркер выбранной
│▌ Двиг10:53 КИП91%   │
│▌ Движ25% вдвиж3:03  │  ← 6 метрик за выбранную смену
│▌ объект717м 156км   │
│▌ [таблица рейсов ▾] │  ← основной просмотр работы: таблица/диаграмма (lazy)
└────────────────────┘
```
6 метрик: Двигатель (`engineTimeSec` h:mm), КИП (`kipPct`), Движение (`secondaryPct`),
В движении (`movingTimeSec` h:mm), На объекте (`onsiteMin`), Пробег (`distanceKm`).
ДСТ-ветка: вместо В движении/На объекте/Пробег → `fuelConsumedL`/`totalStayTimeH` (idle отложен).
Просмотр работы — переиспользуем `ShiftSubTable` + Gantt из v1.

### 3.3 Состояния карточки
- Работала + данные есть → полная карточка.
- Не работала / нули → «не работал».
- Работала, но детали не выгружены → серая диаграмма + кнопка «Выгрузить».

---

## 4. Модель данных (подтверждено чтением `types.ts:44-109`)

| Метрика карточки | Поле `UnifiedRecord` |
|---|---|
| Двигатель 10:53 | `engineTimeSec` |
| КИП 91% | `kipPct` |
| Движение 25% | `secondaryPct` |
| В движении 3:03 | `movingTimeSec` |
| На объекте 717м | `onsiteMin` |
| Пробег 156.4 км | `distanceKm` |

`UnifiedVehicleRow.records[]` хранит пер-сменные `UnifiedRecord`. И верхний таймлайн (rollup по
день×смена×группа), и пер-сменная статистика карточки, и личный спарклайн деривируются клиентски из
одного `fetchUnifiedData(день−13, день)`. **Изменений бэкенда нет.** ДСТ-поля
(`movingTimeSec`/`onsiteMin`/`distanceKm`) для бульдозеров `undefined` → ветвление по `source` (как в v1).

---

## 5. Пошаговый план реализации (вариант A)

1. **Скаффолд (изолированно, без смены поведения):** `viewMode` тип `+ 'cardsV2'`; 4-я кнопка
   «Карточки v2» в тулбаре; рендер-блок `viewMode === 'cardsV2'`; скрыть date-picker в v2;
   расширить условия `AnalyticsPage` (`:577`, `:589`) на `cardsV2`. Создать
   `AnalyticsCardsViewV2.tsx` + `VehicleCardV2.tsx` копиями (рендерят как v1). `npm run lint` чисто.
2. **Данные v2:** собственный фетч 14д + `selectedShift` state (дефолт — последняя доступная смена).
3. **`DayTimelineNav.tsx`:** 28 ячеек, флот %КИП + count, выбор, дне-группировка.
4. **Редизайн `VehicleCardV2`:** гейдж + health-стрипа + 6 метрик + личный спарклайн +
   work-detail (таблица рейсов / диаграмма, lazy с «Выгрузить»).
5. **Связка:** `selectedShift` → карточки показывают эту смену; «не работал»; объект из сайдбара.
6. **CSS** `sv-v2-*` (namespaced, не драться со стилями v1).
7. **Lint + ручная проверка.**

---

## 6. Отложено (v2.1+)

- **Матрица-B:** ряды = группы ТС, столбцы = день×смена, слева select/unselect групп с
  выезжающими подгруппами. Selected = подсветка всего столбца; активные группы = яркие квадраты в
  своих рядах, остальные приглушены. Виз = **квадратные heat-ячейки** (цвет = %КИП), компактно.
  Анимация раскрытия подгрупп = `grid-template-rows` transition / framer-motion, переиспользуя
  цвет-токены текущих фильтров. Заменит daterange + тип-фильтры.
- Idle-стек «работа/ход/простой» (после DST-fix), дельта к прошлому периоду, рейтинг объектов на
  все 4 view, большой DST-фикс неполных/неверифицированных данных.

---

## 7. Идеи-банк сессии + маппинг на 40 идей

- Гейдж КИП (NEW), health-стрипа (NEW), иконки-метрики (NEW), палитра **green→amber→red** вместо
  green→blue→red (NEW, accessibility — синий в середине читается как «инфо», не «внимание»),
  объект 📍 на карточке (NEW, поле `objectName` было скрыто).
- Личный спарклайн на карточке — расширяет **#11** (sidebar sparkline) на карточку.
- Верхний heat-таймлайн = мини-**#38** (Timeline Heatmap), поглощает идею отдельного хитмапа.
- Навигация по сменам — закрывает **#9** (UI смены не было), но как навигация, не фильтр.
- Кликабельная карточка → детали — **#35 / #10**.
- Кандидаты на v2.1+: пульс критичного КИП **#27**, тултип ⚑ **#31**, интерактивные микробары **#13**.
- Перф (вне этой задачи): batch endpoint **#32** уже сделан (см. `ANALYTICS_PERF_HANDOFF.md`),
  **#1** TanStack Query, **#2** виртуализация, **#40** расщепление `AnalyticsPage`.

---

## 8. Открытые точки

- Сайдбар объектов: проверить существующий компонент object-focus (map-view) для переиспользования.
- Личный спарклайн: ось по сменам (28) — подтвердить читаемость на узкой карточке.

---

## 9. Границы и правила

- **Не ломать v1** (Таблица / Карточки / Карта). Всё аддитивно, CSS namespaced `sv-v2-*`.
- `cd frontend && npm run lint` после каждого этапа — должен быть чистым.
- Следовать стилю окружающего кода; `samosvaly.css` — главный CSS аналитики (классы `sv-*`).
- Управление данными — **только через Admin UI** (точечная довыгрузка — существующий метод), не curl/CLI.

---

## Update 2026-06-10 — реализовано и закоммичено

v2.0 реализован и запушен (ветка `agent/glm/fix-analytics-map-positions-colors`, коммиты `4551d2b` v2 + `1aedc85` свод параллельной работы). Отличия от исходного плана:

- **Данные:** v2 НЕ делает собственный 14-дневный фетч — переиспользует уже загруженный глобальный диапазон (деривирует таймлайн/карточки клиентски). Собственный фетч — отложен.
- **Перф onsite — сделано:** работа видна по дефолту во всех карточках; onsite-диаграммы через batch-преподгрузку сегментов (`fetchShiftSegmentsBatch`) + direct-режим `ShiftGanttBar` (`segments`) → ноль пер-карточных фетчей. **Delivery (`ShiftSubTable`) остаётся пер-карточным** — batch-эндпоинта для рейсов нет.
- **Связка по объекту** — работает через общий `focusedObjectUid` (сайдбар фильтрует v2), без нового кода.
- **Таймлайн** — единая флотовая полоса (без разбивки по 4 группам), вариант B (матрица) и дельта к прошлому периоду — отложены.
- Полный статус в vault: `Plans/Analytics Cards v2 — Navigation by Days 2026-06-09.md`.
