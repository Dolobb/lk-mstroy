# Shared Bearer-токен для admin-API приложения выдачи топлива (fuel-backend на VPS).
# Значение = ADMIN_BEARER_TOKEN с VPS (/root/.fuel-app-secrets). НЕ префиксовать VITE_ (иначе попадёт в бандл).
FUEL_ADMIN_TOKEN=
